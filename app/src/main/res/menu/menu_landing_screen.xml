<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".LandingScreen">

    <item
        android:id="@+id/action_delete"
        android:title="Edit Profile"
        app:showAsAction="never"
        android:onClick="onEditProfileClick"/>

    <item
        android:id="@+id/action_addNote"
        android:title="Add Note"
        app:showAsAction="never"
        android:onClick="onAddNoteClick"/>

    <item
        android:id="@+id/action_viewActivityHistory"
        android:title="Activity History"
        app:showAsAction="never"
        android:onClick="onViewActivityHistoryClick"/>

</menu>